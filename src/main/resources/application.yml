server:
  port: 8083

spring:
  application:
    name: config-service

  cloud:
    config:
      server:
        git:
          username: Istiyakmi9
          password: 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
          uri: https://github.com/Istiyakmi9/boot_configuration
          clone-on-start: true

eureka:
  client:
    enabled: true
    serviceUrl:
      defaultZone: http://localhost:8080/eureka
